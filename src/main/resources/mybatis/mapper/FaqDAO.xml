<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meomureum.springboot.dao.FaqMapper">

    <select id="getAllFaqs" resultType="com.meomureum.springboot.dto.FaqDTO">
        SELECT * FROM faq_table ORDER BY created_at DESC
    </select>

    <insert id="insertFaq" parameterType="com.meomureum.springboot.dto.FaqDTO">
        INSERT INTO faq_table (
            faq_code, faq_category, faq_title, faq_content, created_at
        ) VALUES (
            #{faq_code}, #{faq_category}, #{faq_title}, #{faq_content}, NOW()
        )
    </insert>

    <delete id="deleteFaq">
        DELETE FROM faq_table WHERE faq_code = #{faq_code}
    </delete>

</mapper>