<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.meomureum.springboot.dao.INoticeDAO">

    <insert id="insertNotice" parameterType="com.meomureum.springboot.dto.NoticeDTO">
        INSERT INTO NOTICE (
            NOTICE_CODE, NOTICE_TITLE, NOTICE_CONTENT, 
            NOTICE_CATEGORY, CREATED_AT
        ) VALUES (
            LPAD(NOTICE_SEQ.NEXTVAL, 7, '0'),
            #{notice_title},
            #{notice_content},
            #{notice_category},
            SYSDATE
        )
    </insert>

    <select id="searchNotices" resultType="com.meomureum.springboot.dto.NoticeDTO">
        SELECT * FROM NOTICE 
        WHERE NOTICE_TITLE LIKE '%' || #{keyword} || '%' 
           OR NOTICE_CONTENT LIKE '%' || #{keyword} || '%'
        ORDER BY CREATED_AT DESC
    </select>

    <select id="getAllNotices" resultType="com.meomureum.springboot.dto.NoticeDTO">
        SELECT * FROM NOTICE ORDER BY CREATED_AT DESC
    </select>

    <select id="getNoticeDetail" resultType="com.meomureum.springboot.dto.NoticeDTO">
        SELECT * FROM NOTICE WHERE NOTICE_CODE = #{notice_code}
    </select>

    <delete id="deleteNotice">
        DELETE FROM NOTICE WHERE NOTICE_CODE = #{notice_code}
    </delete>

</mapper>